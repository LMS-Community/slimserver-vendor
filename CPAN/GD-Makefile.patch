--- Makefile.PL.orig	2009-07-10 14:10:07.000000000 -0400
+++ Makefile.PL	2010-09-09 11:59:12.000000000 -0400
@@ -225,6 +225,15 @@
 print F $DEFINES,"\n";
 close F;
 
+# Patch to build statically
+@LIBS = ( '-liconv', '-lz', '-lm' );
+push @LIBS, ( '-lgd_s -lfreetype_s' );
+
+my $MYEXTLIB = join( ' ', (
+    '../build/lib/libgd.a',
+    '../build/lib/libfreetype.a',
+) );
+
 WriteMakefile(
     'NAME'	=> 'GD',
     'VERSION_FROM'	=> 'GD.pm',
@@ -251,6 +260,7 @@
     'ABSTRACT'  => 'Interface to Gd Graphics Library',
      $CAPI ? ('CAPI'      => 'TRUE') : (),
     'DEFINE'	=> $DEFINES,
+    'MYEXTLIB' => $MYEXTLIB,
 );
 
 exit 0;
